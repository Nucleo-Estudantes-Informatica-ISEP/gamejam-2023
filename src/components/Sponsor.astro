---
const { image, ref } = Astro.props;
---

<article
  id="sponsor"
  class="w-full aspect-square max-w-xs rounded-lg flex items-center justify-center hover:scale-105 duration-100 ease-in-out"
>
  <a href={ref} target="_blank">
    <img src={image} alt="Sponsor" class="w-60 h-60 object-contain" />
  </a>
</article>
