---
const { image, ref } = Astro.props;
---

<article
  id="sponsor"
  class="w-full aspect-square max-w-xs rounded-lg flex items-center justify-center"
>
  <a href={ref} target="_blank">
    <img
      src={image}
      alt="Sponsor"
      class="w-60 h-60 object-contain hover:scale-105 transition-all duration-200 ease-in-out hover:drop-shadow-whiteShadow"
    />
  </a>
</article>
